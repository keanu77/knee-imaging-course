# 貢獻指南

歡迎 issue 與 PR。這個專案有醫療責任，因此**技術貢獻與醫療內容貢獻適用不同規則**，
請先確認你的變更屬於哪一類。

## 技術貢獻（歡迎直接發 PR）

以下變更只要通過檢查就可以合併：

- 建置腳本、稽核規則、測試（`src/build/`、`tests/`）
- 前端樣式、互動、無障礙與效能（`src/web/`）
- 文件錯字、連結失效、翻譯

送出前請跑：

```bash
make build && make audit && make test
```

三項全綠才會被審。改動前端請一併說明在哪些瀏覽器／螢幕寬度測過。

## 醫療內容貢獻（需要簽核，不會直接上線）

**課程教材、影片清單、逐段筆記、知識檢核題、名詞表**的任何變更，都必須經
具資格醫師逐項簽核才會出現在網站上。這不是流程繁瑣，而是這個站的核心設計：

- 所有內容都有 `review_status`（`draft` / `medical-review` / `approved`）
- `build.py` 只把 `approved` 的內容寫進 `course.json`
- 簽核會寫入 `reviewed_by` / `reviewer_role` / `reviewed_at` / `reviewed_commit`，
  代表某位具名醫師對某個版本的具名背書

因此：

1. **PR 中新增或修改的醫療內容，`review_status` 一律留在 `draft`。**
   不要在 PR 裡把內容改成 `approved`，也不要修改既有的 `reviewed_*` 欄位——
   那等於代替別人簽名。
2. 指出**具體錯誤與依據**：哪個檔案、哪一條、正確的說法是什麼、依據哪份指引或文獻
   （附 PubMed 連結或 DOI）。「我覺得這樣講比較好」無法被審。
3. 修正臨床錯誤（側別、方向、分級判準、藥物或劑量相關敘述）的 issue **優先處理**，
   請直接開 issue 並標明 `medical`，不必等 PR。

### 影片相關的額外要求

新增影片候選必須附上：原始頻道連結（非搬運）、講者姓名與**可查證的資格證據 URL**、
片長與上架日、是否可嵌入、英文字幕是否可核對，以及**片中是否含介入操作及其起始時間**。
含介入的影片需標明 `diagnostic_segment_range`，逐段筆記不得越界（`make test` 會擋）。

不接受：無具名講者的影片、搬運頻道、無字幕無法核對內容者、以介入操作為主體的影片
（本課程為 diagnostic-only）。

## 不接受的貢獻

- 把本課程內容改成**個別病人的醫療建議**或診斷流程指令
- 加入注射、穿刺、水分離等介入操作的操作指引
- 絕對化療效語句（「保證治癒」「完全預防」「百分之百有效」）
- 未經去識別化的病人影像或資料

## 授權

送出貢獻即表示你同意：程式碼部分以 MIT 授權、內容部分以 CC BY-NC-SA 4.0 授權釋出
（見 README 的「授權」一節）。請確認你有權利釋出你所貢獻的內容。
