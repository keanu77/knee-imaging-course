# 2026-09-08 UI／UX 與版型修正

已在本機完成修正與驗證，正式站尚未更新。預覽 http://127.0.0.1:8899/ 。

## 評估與修正

保留紙本圖譜的閱讀方向：暖白背景、深色文字與青綠操作提示。這輪重點是內容到達速度、視覺層級和連續學習操作，不另外更換框架或加入動畫套件。

| 問題 | 實際修正 | 驗證 |
| --- | --- | --- |
| 手機導覽、進度與介紹占據入口 | 導覽改成可展開面板；縮短課程介紹的垂直間距 | 390×900 第一章起點由約 1047 px 提前至 608 px；桌面導覽保持展開 |
| 選片後捲到清單，影片被推離畫面 | 手機選片後回到影片並移轉焦點 | 320×568、390×844、844×390、1012×600 實測；等待平滑捲動完成後量測 |
| 手機筆記與清單互相擠壓，內外捲軸競爭 | 筆記隨頁面捲動；提供固定的影片／筆記／選片快速導覽 | 影片比例、筆記可達、清單操作、框限跳播維持有效 |
| 搜尋摘要沒有套上版型類別，資訊擠成一段 | 分開標題、摘要、來源與片段時間；搜尋時收起重複介紹 | 搜尋結果實際分行，結果仍可直接進入影片片段 |
| 多條件篩選後沒有清楚復原入口 | 一般篩選與空結果都可清除；播放清單另有復原按鈕 | 復原不清除學習紀錄，也不重啟正在看的影片 |
| 長影片標題截斷、桌面按鈕與標題爭空間 | 標題和動作列可換行，筆記字級提高 | 1013×500、1440×600 無水平溢出；筆記可捲動 |
| 共享影片在清單與首頁的計數不一致 | 區分 36 支不重複影片和 38 個影片項目；原片總長依影片 ID 去重 | 原片總長 12 小時 43 分；標示為原片全長，不冒充診斷選段的學習時數 |
| 新運動傷害單元容易讓學員誤認影片涵蓋所有內容 | 加入「影片涵蓋範圍」、原學會病例來源；測驗提交後顯示題目來源 | 四個單元與靜態閱讀頁都能顯示範圍；測驗3題與來源顯示通過 |

新增教材已依使用者「可以收錄」整合：4 單元、4 支影片、22 段、12 題、7 篇文獻。總計18單元、36支影片、354段、60題、31筆參考資料。既有核准教材與審閱戳記保持原值；策展決定與受審 payload hash 見 `course/research/2026-09-08-sports-ultrasound-approval.json`。

## 驗證結果

- `make jscheck test build audit PY=.venv/bin/python` 通過；`.venv/bin/ruff check .` 和 `git diff --check` 通過。
- `tests/browser_smoke.cjs` 全部14情境通過：首頁、搜尋、深連結、歷史導覽、播放器恢復、鍵盤焦點、快捷鍵偏好、無JavaScript閱讀、資料錯誤復原、共用影片單元識別、手機導航、四個新單元與測驗。
- 獨立代理完整檢查相關 call chain 並以 Chrome 驗證小螢幕、橫向與斷點。唯一確認問題是38個影片項目誤稱38支影片（信心0.95），已修正。捲動動畫未完成時的座標負值已排除為測試時序誤報。
- YouTube 在瀏覽器回歸使用無內容 iframe fixture；此結果驗證站內操作，不代表實際串流、逐影格聲畫審查或完整無障礙認證。
- 結構稽核有1項提示：Pedret 原片83:32超過建議70分鐘，已保持真實時長；收錄診斷範圍21:28。未降低稽核門檻。

未 commit、push 或部署。正式站上線後仍需另驗證線上版本標記，不能以本機通過代替部署驗收。
