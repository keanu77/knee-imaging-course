# 2026-09-08 本機優化與教材擴充紀錄

最新狀態：使用者已回覆「可以收錄」；4個新單元已整合，本機共18單元、36支影片、354段、60題。最新UI／UX修正與14項瀏覽器驗證見 `2026-09-08-ui-ux.md`。未commit、push、merge或發布正式站。

以下保留收錄核准前的歷史工作紀錄，當時的draft／待裁決敘述不代表目前狀態。

## 已完成的網站改善

- 修復手機導覽遮擋、播放器離開後返回空白、初訪單元深連結及歷史導覽。
- 診斷片段採共用範圍政策，支援不連續片段、鍵盤與筆記跳播、播放位置保存及非自動播放恢復。精確驗證播放器訊息來源。
- 同一影片用於不同單元時，分享連結保存 `unit`，續播識別保存影片及單元 ID，播放秒數按單元分開；舊數字索引及純影片 ID 記錄仍可遷移。播放清單以 `aria-current` 標示目前影片。
- 搜尋以教材資料與逐段筆記建索引；排除隱藏答案解析，顯示命中摘要與片段入口。中英同義詞與播放器一致。
- 續讀會前往未完成單元，待複習有入口；影片在檢核題之前。章節代碼一致但保留原本 14 個穩定單元 ID。
- 展開狀態、原生控制、焦點、快捷鍵停用、篩選狀態與主題記憶改善。介面改為較易閱讀的圖譜風格、減少無必要英文及固定浮層。
- 14 個已核准單元生成可不執行 JavaScript 的閱讀頁；首頁停用 JavaScript 時仍有閱讀連結。Sitemap 納入閱讀頁，省略無依據的每日 lastmod。
- 建置依 YouTube ID 對應筆記；衝突內容、缺漏／無效影片網址及配置章節內未核准單元會中止建置，保留原產物。未降低 audit 門檻。

## 新教材審閱包

`course/research/2026-09-08-sports-ultrasound-candidates.json` 包含：

- 4 個 draft 單元：伸膝肌腱、側副韌帶、外側痛／彈響、遠端腿後肌／腓腸肌。
- 4 支具名醫師影片（AMSSM Rao／Onishi／Novakofski，及 Pedret 原講者片源）。
- 22 個診斷片段，提出的範圍總長 57:24；12 題及解析、7 篇新文獻、原始病例與資格出處。
- 三支 2020 年影片需要經典例外裁決。SMUG 未具名短片、Sonosite unlisted 及其他重疊來源保留研究歷程，未列為本批建議收錄。
- 內容維持 draft。`python3 tools/build_sports_review.py` 產生 `.tmp/review/sports-ultrasound/index.html` 與 JSON 副本；不混入正式 dist。

## 模型審查與驗證

- Codex、Grok、AGY、Qwen 四份有效報告已保存在 `2026-09-08-model-reports/`。外部模型只讀從公開 GitHub 下載的來源快照；Qwen 在本機執行。
- 第二輪 50 項 finding 逐項裁決，見 `2026-09-08-multi-llm-validation.md`。模型誤報、潛在風險與實測缺陷分開；不直接採用模型嚴重度。
- `make jscheck test build audit PY=.venv/bin/python`、`.venv/bin/ruff check .`、`git diff --check` 通過。新測試覆蓋播放政策、同義詞、建置阻擋、審閱台與靜態閱讀頁；既有 68 項測試保留。
- 最終瀏覽器回歸命令在 `tests/BROWSER_SMOKE.md`；12 項情境全數通過。YouTube fixture 只驗站內行為，不代表真實串流或零越界。
- 另以真實 Chrome 播放 Pedret 與 Rao 兩段，視覺抽查分別看到小腿掃描及股四頭肌腱影像。未逐影格或完整聽審所有影片，起訖視聽裁決仍待策展人完成。
- 桌機／手機 light/dark 畫面、主題儲存與文字對比抽查通過；沒有宣稱完整 WCAG 合規、Core Web Vitals 或真實使用者效能評分。
- 審閱台以 390／1440px 檢查：4 單元、12 題預設收合解析、4 iframe、22 個按鈕的 start/end、題目來源連結及零水平溢出均通過；產物 JSON 與受審原檔完全相同。
- 原 `syllabus.json`、`segments.json`、`questions.json`、`glossary.json`、`video-meta.json` 與 HEAD 位元組相同，既有審閱戳記未改。

## 下一步

由策展人檢視審閱台，裁決四個新單元、片段起訖及三個經典例外。依核准結果轉成正式課程 schema、更新配額及 metadata、填入真實策展紀錄，再跑完整檢查。發布／推送須依使用者下一步授權；目前線上站尚未包含本輪本機改動。

## 完成狀態複核

四模型有效報告與50項逐一裁決已存在；網站本機修正、建置閘門、12項瀏覽器情境均有直接證據。原正式教材與ID未變。新增教材是可閱讀、可操作的draft審閱包，未獲策展裁決，尚未轉入正式課程；本輪網站修正也未發布。整體目標仍未完成，不能把本機測試通過當成正式站已更新。

本次續作屬實質進度：修復了共用影片的單元脈絡、分開續播秒數、補播放清單讀屏狀態，並新增實際瀏覽器及函式回歸證據。其餘待辦為已提出的策展裁決與後續發布授權。
